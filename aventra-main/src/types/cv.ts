/**
 * Folder: src/types
 * File: cv.ts
 * Purpose:
 * - TypeScript types for CV analysis and RAG domain.
 * - Describes payloads, statuses, and result models.
 *
 * Example (when implementing later):
 * - type CvAnalysisResult = { id: string; score: number; summary: string }
 */
