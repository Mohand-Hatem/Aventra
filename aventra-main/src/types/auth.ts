/**
 * Folder: src/types
 * File: auth.ts
 * Purpose:
 * - TypeScript types for authentication domain.
 * - Includes payload and user/token contracts.
 *
 * Example (when implementing later):
 * - type LoginPayload = { email: string; password: string }
 * - type AuthUser = { id: string; role: "user" | "company" }
 */
