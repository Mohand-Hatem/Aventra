/**
 * Folder: src/types
 * File: api.ts
 * Purpose:
 * - Shared API response types and generic wrappers.
 * - Keeps response contracts consistent across queries.
 *
 * Example (when implementing later):
 * - type ApiResponse<T> = { data: T; message: string }
 */
