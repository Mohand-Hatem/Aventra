/**
 * Folder: src/queries
 * File: auth.ts
 * Purpose:
 * - React Query auth hooks (login, register, current user).
 * - Owns server-state for authentication endpoints.
 *
 * Example (when implementing later):
 * - useLoginMutation -> POST /auth/login via apiClient.
 * - useCurrentUserQuery -> GET /auth/me.
 */
