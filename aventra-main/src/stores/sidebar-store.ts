/**
 * Folder: src/stores
 * File: sidebar-store.ts
 * Purpose:
 * - Zustand store for sidebar UI state only.
 * - Controls open/close toggle behavior.
 *
 * Example (when implementing later):
 * - useSidebarStore(state => state.isOpen)
 * - useSidebarStore.getState().toggle()
 */
