/**
 * Folder: src/app/user
 * File: layout.tsx
 * Purpose:
 * - Shared layout for all "/user/*" routes.
 * - Hosts user navigation (profile, upload-cv, reports).
 *
 * Example (when implementing later):
 * - Render sidebar links to user sections.
 * - Render {children} in main content area.
 */
