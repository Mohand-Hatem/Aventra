/**
 * Folder: src/schemas
 * File: cv.ts
 * Purpose:
 * - Zod validation schemas for CV upload and RAG search inputs.
 * - Ensures valid user input before mutation calls.
 *
 * Example (when implementing later):
 * - const uploadCvSchema = z.object({ file: z.instanceof(File) })
 */
