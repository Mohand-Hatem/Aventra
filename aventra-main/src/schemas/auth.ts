/**
 * Folder: src/schemas
 * File: auth.ts
 * Purpose:
 * - Zod validation schemas for auth forms.
 * - Validates login/register payload shape before requests.
 *
 * Example (when implementing later):
 * - const loginSchema = z.object({ email: z.email(), password: z.string().min(8) })
 */
